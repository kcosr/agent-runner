{
  "name": "agent-runner",
  "version": "0.1.0",
  "type": "module",
  "description": "CLI and local daemon host for agent-runner.",
  "license": "MIT",
  "files": ["dist/"],
  "bin": {
    "agent-runner": "./dist/cli.js"
  },
  "scripts": {
    "build": "npm run build:dist && node ../../scripts/copy-web-assets.mjs",
    "build:dist": "tsc -b",
    "prepack": "npm run build -w @agent-runner/core && npm run build -w @agent-runner/web && npm run build:dist && node ../../scripts/copy-web-assets.mjs"
  },
  "dependencies": {
    "@agent-runner/core": "*",
    "ws": "^8.20.0",
    "zod": "^3.23.8"
  }
}
