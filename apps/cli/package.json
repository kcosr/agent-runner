{
  "name": "task-runner",
  "version": "0.1.0",
  "type": "module",
  "description": "CLI and local daemon host for task-runner.",
  "files": ["dist/"],
  "bin": {
    "task-runner": "./dist/cli.js"
  },
  "scripts": {
    "build": "npm run build:dist && node ../../scripts/copy-web-assets.mjs",
    "build:dist": "tsc -b",
    "prepack": "npm run build -w @task-runner/core && npm run build -w @task-runner/web && npm run build:dist && node ../../scripts/copy-web-assets.mjs"
  },
  "dependencies": {
    "@task-runner/core": "*",
    "ws": "^8.20.0"
  }
}
