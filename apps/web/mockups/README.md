This HTML/CSS pair is the visual contract for the phase-1 run dashboard in `apps/web`. It was copied from the planning draft artifacts under `${TASK_RUNNER_STATE_DIR}/drafts/task-runner/` and is the authority for the board-plus-detail layout, spacing, hierarchy, and chrome in this slice.

Material differences in the React implementation must be justified in the relevant task notes. The mock governs layout, column behavior, card content, drawer structure, filter chrome, and the visual language: monochrome surfaces, status-as-tinted-badge, and no decorative color.
