---
schemaVersion: 1
id: feature-implement/implement-tests
title: Add and update tests
---
**Category**: code-bearing

**Done when:** Tests cover the behavior introduced or changed by `feature-implement/implement-core`, including happy paths, error paths, edge cases, and representative existing sibling behaviors for any shared path that changed.

Use the test strategy from `feature-plan/risks-and-tests`. Update existing tests whose assumptions changed, and add integration coverage for user-facing surfaces from `feature-plan/surface-inventory`.

Paste test file paths, specific test names, focused test commands, and exit codes into Notes.
