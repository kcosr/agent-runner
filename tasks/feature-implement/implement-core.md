---
schemaVersion: 1
id: feature-implement/implement-core
title: Implement core feature logic
---
**Category**: code-bearing

**Done when:** The approved contract from `feature-plan/contract` is implemented end-to-end, every Surface Inventory entry has a real runtime path, and no implementation scope promised by `assignment-summary.md` remains only in prose.

Use the earlier planning Notes as the canonical implementation plan:
  - Feature brief: `feature-plan/capture-feature`
  - Impact survey: `feature-plan/survey-impact`
  - Existing-code check: `feature-plan/check-existing-code`
  - Contract: `feature-plan/contract`
  - Surface Inventory: `feature-plan/surface-inventory`

Cite every file you edit or create in Notes. Do not leave TODO comments or silent partial implementations. If the approved contract is wrong or infeasible, mark this task `blocked` with the exact conflict instead of diverging silently.
